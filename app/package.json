{
  "name": "gitops-api",
  "version": "2.0.0",
  "description": "GitOps API with Prometheus metrics",
  "main": "src/server.js",
  "scripts": {
    "start": "bun run src/server.js",
    "dev": "bun --watch src/server.js",
    "test": "bun test"
  },
  "dependencies": {
    "express": "^4.21.0",
    "pg": "^8.13.0",
    "ioredis": "^5.4.0",
    "prom-client": "^15.1.0"
  }
}
